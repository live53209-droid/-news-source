# Copy this file to dzen/.env and fill in your values
# The config.py will automatically load it (if python-dotenv is installed)

# OpenAI (ChatGPT) - current LLM for Dzen articles
OPENAI_API_KEY=sk-proj-your-key-here
OPENAI_MODEL=gpt-4o-mini    # Current default - good quality/cost balance for Dzen

# Optional: you can also use gpt-4o-mini for cheaper generation
# OPENAI_MODEL=gpt-4o-mini